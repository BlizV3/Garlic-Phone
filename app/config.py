# ── Server configuration ──────────────────────────────────────────────────────
# Set RENDER_URL to your Render WebSocket URL once deployed.
# Example: "wss://garlic-phone-server.onrender.com"
# Leave as empty string to use local server mode (LAN play).

RENDER_URL = "wss://garlic-phone-xyz.onrender.com"   # ← paste your Render URL here after deploying

# Port used for local server mode
LOCAL_PORT = 8765
