services:
  - type: web
    name: garlic-phone-server
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python server_main.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0